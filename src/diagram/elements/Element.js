/*

(function() {
    Kinetic.Util.addMethods(Kinetic.Element, {
        _initElement: function(config) {
            this.nodeType = 'Element';
            this.createAttrs();
            // call super constructor
            Kinetic.Group.call(this, config);
        }
    });
    Kinetic.Util.extend(Kinetic.Element, Kinetic.Group);
})(); 

*/